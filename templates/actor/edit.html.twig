{% extends 'base.html.twig' %}

{% block title %}Edit Actor{% endblock %}

{% block body %}
    <section class="card text-dark m-3">
        <div class="card-header bg-secondary">
            <h1>Mettre à jour l'acteur {{ actor.name }}</h1>
        </div>
        <div class="col-sm-12 p-3">
            {{ include('actor/_form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </section>
    <a class="btn btn-secondary" href="{{ path('actor_index') }}">Retour aux acteurs</a>
{% endblock %}
